machine:

  timezone: America/New_York
  node.version: 0.10.28
  php.version: 5.3.3

  environment:
    NODE_ENV: development
    PHP_ENV: development

dependencies.pre:
  - echo "memory_limit = 512M" > ~/.phpenv/versions/$(phpenv global)/etc/conf.d/memory.ini

notify.webhooks:
  - url: "http://208.52.164.217/webhook.php"

general.branches.ignore:
  - gh-pages
